# Observatory identity

The primary interface mark is the supplied blue-eye artwork. The application embeds that artwork in the dedicated `app/brand-eye.css` identity layer, scales the mark up for mobile readability, and adds only subtle decorative motion. Reduced-motion preferences disable the animation.

The identity layer is intentionally separate from evidence visualizations: relationship, replay, geography, and source-state motion retain their own evidence semantics.
